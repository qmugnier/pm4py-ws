This project contains some submodules.

Hence, once you take this from the Github, please do the following commands:

git submodule init
git submodule update


----------------------------------------

At every compilation, please enter the webapp2 folder and execute the following commands:

git checkout master
git pull